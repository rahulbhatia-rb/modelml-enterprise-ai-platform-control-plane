# Observability & On-call

Every production workload should expose:
- metrics for saturation, errors, traffic and latency
- structured logs with tenant-safe redaction
- traces across service boundaries
- correlation IDs
- deployment markers
- actionable alerts with an owner and runbook

Page on symptoms that require human action, not raw infrastructure noise.
